// Navigation is now handled by AppSidebar (desktop) + MobileNav (mobile)
// via the (app) layout. This component is kept as a no-op so the 16 page
// files that import it don't need to be touched.

export function Navbar() {
  return null;
}
